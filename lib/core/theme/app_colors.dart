import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ============================================
  // Surface / Background
  // ============================================
  static const Color surfaceMain = Color(0xFF101C26);
  static const Color surfaceCard = Color(0xFF192936);
  static const Color surfaceInput = Color(0xFF213647);

  // ============================================
  // Brand / Action
  // ============================================
  static const Color brandPrimary = Color(0xFF1A9BFF);
  static const Color brandDeep = Color(0xFF0F3A5F);
  static const Color brandGlow = Color(0xFF0C4A75);

  // ============================================
  // Content / Neutral
  // ============================================
  static const Color contentHigh = Color(0xFFFDFDFD);
  static const Color contentMedium = Color(0xFFA9B4C2);
  static const Color contentLow = Color(0xFF8D8E92);

  // ============================================
  // Border
  // ============================================
  static const Color borderDefault = Color(0xFF334155);

  // ============================================
  // Status - Success
  // ============================================
  static const Color successText = Color(0xFF48DD7F);
  static const Color successBgDark = Color(0xFF224A44);

  // ============================================
  // Status - Danger
  // ============================================
  static const Color dangerText = Color(0xFFBF595A);
  static const Color dangerBgDark = Color(0xFF3D2329);

  // ============================================
  // Tetris Block Colors
  // ============================================
  /// I (Line) - Cyan/Biru terang
  static const Color blockI = Color(0xFF1A9BFF);

  /// O (Square) - Yellow
  static const Color blockO = Color(0xFFFACC15);

  /// T - Purple
  static const Color blockT = Color(0xFFA855F7);

  /// S - Green
  static const Color blockS = Color(0xFF22C55E);

  /// Z - Red
  static const Color blockZ = Color(0xFFEF4444);

  /// J - Blue
  static const Color blockJ = Color(0xFF2563EB);

  /// L - Orange
  static const Color blockL = Color(0xFFF97316);
}
